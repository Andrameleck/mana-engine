% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_reference_lotusnoir.R
\name{query_lotusnoir_download_decks_sqlite}
\alias{query_lotusnoir_download_decks_sqlite}
\title{Download LotusNoir Decks to SQLite}
\usage{
query_lotusnoir_download_decks_sqlite(
  db_path = "",
  max_decks = 100L,
  request_delay = 5,
  refresh_urls = FALSE,
  probe_radius = 0L,
  probe_max_dids = 0L,
  overwrite_existing = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{db_path}{Character scalar. Path to the SQLite database file.
Created if it does not exist.}

\item{max_decks}{Integer. Maximum number of decks to download (default \code{100}).}

\item{request_delay}{Numeric. Seconds to wait between HTTP requests
(default \code{5}).}

\item{refresh_urls}{Logical. If \code{TRUE}, clears cached URLs before
re-discovering them (default \code{FALSE}).}

\item{probe_radius}{Integer. Extra index pages to probe beyond the default
set (default \code{0}).}

\item{probe_max_dids}{Integer. Cap on additional deck IDs to probe
(default \code{0}).}

\item{overwrite_existing}{Logical. Re-download decks already in the database
(default \code{FALSE}).}

\item{verbose}{Logical. Print progress messages (default \code{TRUE}).}
}
\value{
A list with \code{ok} (logical), \code{n_downloaded} (integer),
\code{n_skipped} (integer), and \code{db_path} (character).
}
\description{
Crawls LotusNoir deck pages and stores card data in a local SQLite database.
Can be run incrementally: existing decks are skipped unless
\code{overwrite_existing = TRUE}.
}
