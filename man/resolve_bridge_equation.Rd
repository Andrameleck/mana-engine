% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge_synergy.R
\name{resolve_bridge_equation}
\alias{resolve_bridge_equation}
\title{Resolve A + n*k + B Bridge Equations}
\usage{
resolve_bridge_equation(
  cards,
  A_id,
  B_id,
  n = 2L,
  depth_N = 3L,
  top_k = 50L,
  gamma = 0.65,
  topN = 20L,
  known_synergies = list(),
  reference_bonus = 0.3,
  near_bonus = 0.18,
  novel_penalty = 0.05,
  missing_card_penalty = 0.03,
  max_missing_cards = 2L,
  options = list(enforce_color_identity = FALSE, unique_nodes = TRUE, max_candidates =
    2000L, max_chain_len = 6L, support_k = 2L)
)
}
\arguments{
\item{cards}{List of cards.}

\item{A_id}{Seed A id.}

\item{B_id}{Seed B id.}

\item{n}{Maximum number of bridge cards \code{k} to keep between A and B.}

\item{depth_N}{Propagation depth.}

\item{top_k}{Neighbor and frontier cap.}

\item{gamma}{Damping factor.}

\item{topN}{Number of bridge cards to keep.}

\item{known_synergies}{List of known synergy references. Each item should
contain \code{cards}.}

\item{reference_bonus}{Bonus applied to exact known matches.}

\item{near_bonus}{Bonus applied to near matches.}

\item{novel_penalty}{Mild penalty applied when no known match is found.}

\item{missing_card_penalty}{Penalty per missing card for near matches.}

\item{max_missing_cards}{Maximum missing cards allowed for a near match.}

\item{options}{Options forwarded to \code{\link[=compute_bridge_recommendations]{compute_bridge_recommendations()}}.}
}
\value{
List with \code{candidates}, \code{references}, and the raw \code{bridge_payload}.
}
\description{
Build candidate bridge packages between two seed cards, then re-rank them
against known synergy references such as Commander Spellbook or curated
Scryfall-derived packages.
}
\details{
Each known reference is a list with at least a \code{cards} field and optional
metadata such as \code{source}, \code{label}, and \code{weight}.
}
\examples{
cards <- bridge_synergy_example_cards()
refs <- list(
  list(
    label = "Known glue package",
    source = "spellbook",
    cards = c("card_A", "card_glue", "card_B"),
    weight = 1
  )
)
out <- resolve_bridge_equation(cards, "card_A", "card_B", n = 2L, known_synergies = refs)
stopifnot(is.data.frame(out$candidates))

}
