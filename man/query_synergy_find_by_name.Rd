% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_synergy_find_by_name.R
\name{query_synergy_find_by_name}
\alias{query_synergy_find_by_name}
\title{Find synergies for a card by name}
\usage{
query_synergy_find_by_name(
  card_name,
  format = "commander",
  max_results = 10L,
  package_top_n = 6L,
  top_k = NULL,
  cheap_scan_cap = 1500L,
  cards = NULL
)
}
\arguments{
\item{card_name}{Character scalar. Name of the seed card.}

\item{format}{Character scalar. Format used for legality / format-fit axis
(default \code{"commander"}).}

\item{max_results}{Integer. Number of best matches to return (default \code{10}).}

\item{package_top_n}{Integer. Number of packages to return (default \code{6}).}

\item{top_k}{Integer. Internal candidate cap forwarded to the engine
(default \code{max(24, max_results * 3)}).}

\item{cheap_scan_cap}{Integer. Cheap pre-filter cap forwarded to the engine
(default \code{1500}).}

\item{cards}{Optional pre-loaded catalog (list of normalized cards). When
\code{NULL} (default) the function calls \code{\link[=query_synergy_get_catalog]{query_synergy_get_catalog()}}.}
}
\value{
A list with the following entries:
\describe{
\item{\code{ok}}{Logical scalar.}
\item{\code{seed}}{Named character vector: \code{name}, \code{id}.}
\item{\code{source}}{Catalog source label (e.g. \code{"sqlite_cards"}).}
\item{\code{catalog_size}}{Integer.}
\item{\code{elapsed_s}}{Numeric, seconds spent scoring.}
\item{\code{matches}}{Data.frame with columns \code{rank}, \code{name}, \code{score},
\code{reasons} (semicolon-joined character).}
\item{\code{packages}}{Data.frame with columns \code{rank}, \code{score}, \code{members}
(\code{+}-joined character).}
}
On error returns \verb{list(ok = FALSE, error = <message>)}.
}
\description{
Convenience wrapper around \code{\link[=query_synergy_find_in_catalog]{query_synergy_find_in_catalog()}} that loads the
default catalog (sqlite-backed) on first call, runs the synergy pipeline for
the requested seed card, and returns the result as tidy data.frames suitable
for inspection in the R console or further processing.
}
\examples{
\dontrun{
res <- query_synergy_find_by_name("Entomb")
res$matches
res$packages
}
}
