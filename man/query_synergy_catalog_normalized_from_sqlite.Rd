% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synergy_enriched_sqlite.R
\name{query_synergy_catalog_normalized_from_sqlite}
\alias{query_synergy_catalog_normalized_from_sqlite}
\title{Load already-normalized cards from the enriched SQLite catalog.}
\usage{
query_synergy_catalog_normalized_from_sqlite(
  db_path = NULL,
  source_label = "enriched_sqlite"
)
}
\arguments{
\item{db_path}{Path to the SQLite file holding \code{cards_enriched}. Defaults
to the per-user cache \code{all_cards.sqlite}.}

\item{source_label}{Logical source name attached as \code{synergy_source}.
Defaults to \code{"enriched_sqlite"}.}
}
\value{
A list of normalized cards, or a \code{query_api_error()} payload.
}
\description{
Returns a list of \code{normalized_card_v2} records (skipping any rows whose
\code{oracle_id} and \code{name} are both empty). Each call validates that
\code{enrichment_meta.precompute_version} matches
\code{query_synergy_precompute_version()}; mismatches yield a
\code{query_api_error()} so callers can fall back to live normalisation.
}
\details{
The returned list carries the same \verb{synergy_*} attributes that
\code{query_synergy_catalog_from_sqlite()} attaches, plus
\code{synergy_normalized = TRUE} so \code{query_synergy_get_precomputed_catalog()}
knows it can skip the per-card normalisation pass.
}
