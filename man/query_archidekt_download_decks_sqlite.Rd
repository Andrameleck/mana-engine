% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_reference_archidekt.R
\name{query_archidekt_download_decks_sqlite}
\alias{query_archidekt_download_decks_sqlite}
\title{Download a range of Archidekt decks into a SQLite database}
\usage{
query_archidekt_download_decks_sqlite(
  db_path,
  deck_ids = NULL,
  min_id = 1L,
  max_id = 9000000L,
  request_delay = 0.1,
  overwrite = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{db_path}{Path to the SQLite file. Created if absent.}

\item{deck_ids}{Optional integer vector of specific IDs to fetch.
If NULL, scans \code{min_id}:\code{max_id}.}

\item{min_id}{Lower bound for sequential scan. Default 1.}

\item{max_id}{Upper bound for sequential scan. Default 9000000.}

\item{request_delay}{Seconds to wait between requests. Default 0.1.}

\item{overwrite}{If TRUE, re-fetches decks already in the database.}

\item{verbose}{Print progress messages. Default TRUE.}
}
\value{
List with ok, db_path, inserted, skipped, errors.
}
\description{
Scans deck IDs from \code{min_id} to \code{max_id} (or a provided vector)
and stores each valid public deck in a SQLite database with two tables:
\describe{
\item{decks}{One row per deck: id, url, title, author, format, updated_at}
\item{deck_cards}{One row per card slot: deck_id, section, quantity, card_name, scryfall_id}
}
}
\details{
Progress is checkpointed so interrupted runs can resume.
}
