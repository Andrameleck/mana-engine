% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge_synergy.R
\name{propagate_scores}
\alias{propagate_scores}
\title{Propagate Hierarchical Synergy Scores from a Seed}
\usage{
propagate_scores(
  seed_id,
  cards,
  depth_N = 3L,
  top_k = 50L,
  gamma = 0.65,
  options = list()
)
}
\arguments{
\item{seed_id}{Seed card id.}

\item{cards}{List of cards.}

\item{depth_N}{Propagation depth (>= 1).}

\item{top_k}{Top-k neighbors per parent and frontier cap per level.}

\item{gamma}{Depth damping factor in \link{0, 1}.}

\item{options}{List of options:
\code{enforce_color_identity}, \code{unique_nodes}, \code{max_candidates}.}
}
\value{
List with \code{scores}, \code{parents}, and \code{stats}.
}
\description{
Expand from a seed card over \code{depth_N} levels. At each level, each frontier
node emits to its weighted top-k neighbors and contributions are damped by
\code{gamma^depth}.
}
\examples{
cards <- bridge_synergy_example_cards()
out <- propagate_scores("card_A", cards, depth_N = 2L, top_k = 5L, gamma = 0.65)
stopifnot(is.numeric(out$scores), is.character(out$parents))

}
