% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synergy_catalog_sqlite.R
\name{query_synergy_catalog_from_sqlite}
\alias{query_synergy_catalog_from_sqlite}
\title{Load a synergy catalog from a local SQLite database}
\usage{
query_synergy_catalog_from_sqlite(
  db_path = NULL,
  table = "cards_api",
  where = NULL,
  limit = NULL,
  source_label = "sqlite_cards",
  dedupe_by_name = TRUE
)
}
\arguments{
\item{db_path}{Path to the SQLite file. Defaults to the per-user cache
location (\code{tools::R_user_dir("mana-engine","cache")/all_cards.sqlite}).}

\item{table}{Table name to query. Defaults to \code{"cards_api"}.}

\item{where}{Optional SQL \code{WHERE} clause (without the keyword) to restrict
the rows pulled (for example \code{"lang = 'en'"}).}

\item{limit}{Optional integer row cap.}

\item{source_label}{Logical source name attached as \code{synergy_source} on the
returned list. Defaults to \code{"sqlite_cards"}.}

\item{dedupe_by_name}{Logical. If \code{TRUE} (default), keep only the first row
per card name when the table contains duplicates.}
}
\value{
A list of card records on success, or a \code{query_api_error} payload
when dependencies/files are missing.
}
\description{
Returns a list of trimmed card records ready for \verb{query_synergy_*}
pipelines, with the same attributes (\code{synergy_source}, \code{synergy_cache_key},
\code{synergy_source_signature}) the Scryfall path attaches.
}
