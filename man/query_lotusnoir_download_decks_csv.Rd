% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_reference_lotusnoir.R
\name{query_lotusnoir_download_decks_csv}
\alias{query_lotusnoir_download_decks_csv}
\title{Download LotusNoir Decks to CSV}
\usage{
query_lotusnoir_download_decks_csv(
  output_csv,
  deck_urls = NULL,
  index_urls = NULL,
  max_decks = 100L,
  request_delay = 20,
  append = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{output_csv}{Character scalar. Path to the output CSV file.}

\item{deck_urls}{Character vector. Specific deck URLs to download.
If empty, URLs are discovered from \code{index_urls}.}

\item{index_urls}{Character vector. Index page URLs used for discovery
when \code{deck_urls} is empty. Defaults to standard LotusNoir index pages.}

\item{max_decks}{Integer. Maximum number of decks to download (default \code{100}).}

\item{request_delay}{Numeric. Seconds to wait between HTTP requests
(default \code{20}).}

\item{append}{Logical. Append to existing CSV file (default \code{TRUE}).}

\item{verbose}{Logical. Print progress messages (default \code{TRUE}).}
}
\value{
A list with \code{ok} (logical), \code{n_downloaded} (integer),
\code{n_skipped} (integer), and \code{output_csv} (character).
}
\description{
Crawls LotusNoir deck pages and appends card rows to a CSV file.
Suitable for a lightweight export without SQLite.
}
