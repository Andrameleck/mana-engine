% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge_synergy.R
\name{top_k_neighbors}
\alias{top_k_neighbors}
\title{Top-k Neighbor Cards for One Parent Card}
\usage{
top_k_neighbors(u_idx, cards, feature_index, top_k = 50L, options = list())
}
\arguments{
\item{u_idx}{Integer index of the parent card in \code{cards}.}

\item{cards}{List of cards.}

\item{feature_index}{Output of \code{\link[=build_feature_index]{build_feature_index()}}.}

\item{top_k}{Maximum number of neighbors to return.}

\item{options}{List of options:
\code{enforce_color_identity}, \code{max_candidates}.}
}
\value{
List with \code{indices}, \code{weights}, and \code{candidates_considered}.
}
\description{
Compute top-k weighted neighbors for one card index, with deterministic tie
breaking by ascending card index.
}
\examples{
cards <- bridge_synergy_example_cards()
idx <- build_feature_index(cards)
nn <- top_k_neighbors(1L, cards, idx, top_k = 5L, options = list(max_candidates = 100L))
stopifnot(is.list(nn), length(nn$indices) == length(nn$weights))

}
