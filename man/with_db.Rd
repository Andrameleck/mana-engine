% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_db.R
\name{with_db}
\alias{with_db}
\title{Open, use, and always-disconnect a SQLite database}
\usage{
with_db(db_path, fn)
}
\arguments{
\item{db_path}{Character scalar. Path to the SQLite database file.}

\item{fn}{A function accepting a single \code{DBIConnection} argument.}
}
\value{
The return value of \code{fn(con)}.
}
\description{
Opens a connection to \code{db_path}, passes it to \code{fn}, and
guarantees disconnection via \code{on.exit} — even if \code{fn} throws.
}
\examples{
\dontrun{
  result <- with_db("path/to/db.sqlite", function(con) {
    DBI::dbGetQuery(con, "SELECT 1 AS n")
  })
}
}
