% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synergy_score_pair.R
\name{query_synergy_score_pair_symmetric}
\alias{query_synergy_score_pair_symmetric}
\title{Role-aware bidirectional pair scoring (AGENTS.md § 4.1).}
\usage{
query_synergy_score_pair_symmetric(card_a, card_b, format_name = "commander")
}
\arguments{
\item{card_a}{Normalized card (output of \code{query_synergy_normalize_card}).}

\item{card_b}{Normalized card.}

\item{format_name}{Format used for legality checks (default "commander").}
}
\description{
Wraps \code{query_synergy_score_pair()} to satisfy the direction-invariance
requirement: scores the pair in both orientations, infers a role for each
card that does not depend on which one was supplied as the seed, and
returns a \code{final_pair_score} that is symmetric up to a small documented
bonus for bidirectional reinforcement.
}
\details{
Naming convention: \code{card_a} and \code{card_b} are arbitrary labels for the two
input cards. The output exposes:
\itemize{
\item \code{card_a_role} / \code{card_b_role} : direction-invariant role labels.
\item \code{a_feeds_b_score} / \code{b_feeds_a_score} : the two directional scores
(each is the result of \code{query_synergy_score_pair(producer, consumer)$score}).
\item \code{dominant_relationship} : \code{"a_feeds_b"}, \code{"b_feeds_a"},
\code{"mutual_feedback"}, \code{"anti_synergy"}, or \code{"tool_support"}.
\item \code{final_pair_score} : the strongest meaningful contribution plus a small
reciprocity bonus (capped at 100). NOT a naive average.
\item \code{directional} : the two underlying \code{query_synergy_score_pair()} results
so callers retain full explainability.
}
}
