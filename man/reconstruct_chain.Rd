% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge_synergy.R
\name{reconstruct_chain}
\alias{reconstruct_chain}
\title{Reconstruct an Explainable Seed-to-Target Chain}
\usage{
reconstruct_chain(parents_map, seed_id, target_id, max_len = 6L)
}
\arguments{
\item{parents_map}{Named character vector (id -> parent_id).}

\item{seed_id}{Seed id.}

\item{target_id}{Target id.}

\item{max_len}{Maximum chain length.}
}
\value{
Character vector of ids in order \code{seed -> ... -> target}, or empty.
}
\description{
Reconstruct a chain from \code{seed_id} to \code{target_id} by following parent pointers.
Returns an empty vector if seed is not reached, if a loop is detected, or if
max length is exceeded.
}
\examples{
parents <- c(A = NA, B = "A", C = "B")
ch <- reconstruct_chain(parents, "A", "C", max_len = 5L)
stopifnot(identical(ch, c("A", "B", "C")))

}
